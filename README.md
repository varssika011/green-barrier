# Corrosion Prediction App

This app uses machine learning to predict corrosion rates based on environmental conditions.

## How to run locally
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `streamlit run app.py`
